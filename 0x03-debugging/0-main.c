#include "main.h"
/**
 * main - test a function that print negative of positive
 * positive_or_negative - checks the number
 * Return: 0
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
